# LinkedIn Post #1

## Full version (5–8 lines)
Manual weekly CSV reporting eats hours and introduces avoidable errors.
I built a Python automation demo that takes one raw export and produces clean reporting outputs.
It standardizes messy fields, filters/reporting windows, and generates `weekly_summary.csv`, `top_products.csv`, and a share-ready `weekly_report.xlsx`.
Optional: publish summary tabs directly to Google Sheets.
Proof: the repo includes the pipeline, sample data flow, and exact output files in `data/processed/`.
If you’re doing recurring ops/finance reporting, this cuts copy/paste work fast.
DM me if you want the repo + a quick walkthrough tailored to your export format.

## Short version (2–3 lines)
Still doing weekly CSV reporting manually?
This demo turns one export into clean summaries + a ready-to-share Excel report (and optional Google Sheets publish).
DM me if you want the repo/video and a version adapted to your workflow.
